import AnimatedOutlet from './AnimatedOutlet';

/**
 * Root layout that wraps all top-level routes with animated transitions.
 * This is the outermost layout in the router hierarchy.
 */
export default function RootLayout() {
  return <AnimatedOutlet />;
}
